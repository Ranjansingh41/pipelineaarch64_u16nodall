
language: node_js

# Version number
node_js:
  - 6.14 #.2 
  - 8.11 #.2 
  #- 9.11 #.1 
  - 10.6 #.0

runtime:
  nodePool: Arm64

build:
  pre_ci_boot:
    image_name: drydock/aarch64_u16nodall
    image_tag: master
    pull: true
    
  ci:
    - node --version
    - grunt --version
    - mocha --version
    - vows --version
    - bower --version
    - nvm list
    - npm --version
